<% content_for :page_title do %>
  <%= Spree.t(:new_room) %>
<% end %>

<div data-hook="admin_room_new_form_header">
  <%= render 'spree/shared/error_messages', target: @room %>
</div>

<div data-hook="admin_room_new_form">
  <%= form_for [:admin, @room] do |f| %>
    <div class="row">
      <div class="col-md-12">
        <%= render 'form', f: f %>
        <%= render 'spree/admin/shared/new_resource_links' %>
      </div>
    </div>
  <% end %>
</div>
